package devices;

// Renomeei essa classe de ScannerA somente para não conflitar com a classe java.util.Scanner
// Para não dar problema no auto import
public interface ScannerA {
    String scan();
}
