# Interface Comparable

## Implementação

```java
public interface Comparable<T> {
    int compareTo(T o); // generics
}
```

A interface `Comparable` serve para definir na classe como que um objeto é comparado com outro.
- É somente necessário implementar o método `compareTo` ao implementar a interface `Comparable`.
